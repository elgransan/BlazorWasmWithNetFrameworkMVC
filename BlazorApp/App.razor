@inject IJSRuntime JS

<DynamicComponent Type="@Type.GetType(text)" />

@code {
    private string text = "";

    protected override async Task OnInitializedAsync()
    {
        text = await JS.InvokeAsync<string>("getProperty", "app", "test");
    }
}
